# Design QA — Company Workspace（方案 2）

final result: passed

## 验收范围

- 参考视觉：方案 2，1487 × 1058，按 1440 × 1024 等比归一后比较。
- 实现页面：WebUI 根路由的 Company Workspace。
- 核心路径：频道查找与切换、批准合并、证据 Thread 开合、Thread 工具行展开、消息发送、移动端频道抽屉。
- 数据边界：当前页面使用隔离的 View Model fixtures；本轮不声称已接通公司项目后端事件流。

## 对照材料

- 第一轮对照：`.artifacts/design-qa/comparison-pass-1.png`
- 最终对照：`.artifacts/design-qa/comparison-final.png`
- 最终桌面截图：`.artifacts/design-qa/implementation-final.png`
- 响应式截图：`.artifacts/design-qa/responsive-1180.png`、`.artifacts/design-qa/responsive-820-main.png`、`.artifacts/design-qa/responsive-390-main.png`、`.artifacts/design-qa/responsive-390-drawer.png`

## 已修复发现

1. P2 · 布局：首轮实现的 Rail、频道栏与 Thread 分别为 72/280/324px，导致主会话被明显压窄。已调整为 80/264/300px，与参考图的四栏比例对齐。
2. P2 · 视觉密度：交付卡缺少内部标题行且预览区偏矮，卡片较参考图短约 60px。已增加“交付验收 / 已通过”标题行并提高预览高度，使审批卡与固定输入区的垂直节奏接近参考图。
3. P2 · 图片与人物层级：主会话 Agent 头像过小。已从 32px 提升至 40px，并修正头像容器拉伸。
4. P2 · 状态表达：项目头部缺少与参考图一致的审核状态。已增加“需要批准 / 已批准”状态标签，并与批准动作联动。
5. P1 · 响应式行为：390px 下频道栏被隐藏后没有重新进入的路径，标题与参与者也发生拥挤。已把消息按钮接成频道抽屉入口，频道选择后自动收起，并在手机宽度精简头部次要元素。
6. P2 · 细节：主会话、频道栏和 Thread 的原生滚动条破坏了参考图的轻量表面。已隐藏滚动条但保留滚动能力。

## 最终检查

- 字体与层级：中文与英文回退栈稳定；标题、正文、元数据和状态标签层级清楚，无破损换行。
- 布局与间距：1440 × 1024 下四栏完整，无重叠、裁切或横向滚动；卡片、边框、阴影和圆角与参考视觉一致。
- 颜色与资产：使用暖白底、淡紫选中态、橙色批准态和绿色通过态；全部头像与交付预览均为实际位图资产，图标来自现有组件库。
- 状态与交互：批准后卡片和最终决定同步更新；Thread 可关闭并从证据入口恢复；消息可发送；搜索可过滤频道；移动端抽屉可打开、切换频道并收起。
- 响应式：1180、820 和 390px 视口无水平溢出；窄屏审批卡转为单栏，固定输入区保持可用。
- 可访问性：主要区域具有语义标签；图像有替代文本；输入与图标按钮具有可访问名称；键盘焦点可见；已提供 reduced-motion 降级。
- 工程校验：App 类型检查通过；310 项 App 单元测试通过；生产构建通过。
