# R1–R3 待测试项留档

> 本文件记录按 `Agent-Company-Experience-Refactor-Plan-v1.0.md` 逐项开发的 R1→R2→R3 任务，
> 及每项交付需要**专人测试**的验证点。开发者不执行这些测试，仅负责实现与单元自测；
> 勾选框留给测试同学在真实环境验证后填写。
>
> 状态图例：`[已实现]` 代码已落地并通过开发者单元自测 · `[待测试]` 需专人验证 · `[未开始]`

---

## R1 GOAL-01 — 用户输入意图分类与可纠正路由  `[已实现]`

**实现位置**
- `packages/control-plane/src/conversation/intent.ts`（新增）：确定性纯函数意图分类器。
- `packages/control-plane/src/conversation/intake.ts`（改造）：Board 新消息经分类器门控，不再无条件立项；新增 `intentOverride` 输入与 `intent/intentConfidence/autoProjected/needsIntentConfirmation` 投影输出。
- `packages/control-plane/test/conversation/intent.test.ts`（新增）：105 条中英文样本 + 19 条断言，开发者自测全绿。

**设计要点**
- 六类意图：`casual / question / task / goal / intervention / approval`；仅 `task | goal` 会 `createsProject`。
- 高影响动作高精度优先：`createsProject` 且 `confidence < 0.6` 时转为 `needsConfirmation`，不静默立项。
- 显式护栏（"只讨论/不要执行"）强制走讨论路径。
- 用户可用 `IntentOverride`（execute / discuss / project_followup）显式纠正路由。
- 分类器确定性，重放消息时重新推导投影，无需额外持久化。

**开发者自测结论**：`bun test test/conversation/intent.test.ts test/conversation/intake.test.ts` → 19 pass / 0 fail；`bun typecheck` clean。

### 待专人测试项
- [ ] 在真实 WebUI Board 频道发送闲聊/问候（"你好""谢谢"），确认**不创建项目**、不出现在 Work 列表。
- [ ] 发送知识问题（"什么是向量数据库？""Why is my build slow?"），确认走问答、**不立项**。
- [ ] 发送明确任务（"帮我写一个快速排序函数"），确认**自动立项**并进入 Work。
- [ ] 发送复杂目标（"开发一个完整的待办 Web 应用并部署"），确认识别为 goal 并立项。
- [ ] 发送项目干预（"停止当前项目""继续上一个项目"），确认路由为干预而非新项目。
- [ ] 发送审批回应（"同意""approved""lgtm"），确认识别为 approval、不立项。
- [ ] 发送低置信度短祈使句（"写个脚本"），确认弹出**确认**而非静默立项。
- [ ] 使用"作为目标执行 / 仅讨论 / 追加到已有项目"三种纠正入口，确认 override 生效且可审计。
- [ ] 对抗样本（"帮我想想但先不要执行"），确认**不立项**且不误弹确认。
- [ ] 回归：原有 Board 首条任务型消息仍能正确立项、建线程、拉成员、起 run（intake 兼容性）。
- [ ] 边界：空白/纯标点/超长（接近 20k 字符）消息不崩溃、有确定性回退。

---

## R1 GOAL-03 — 以结果为中心的 Goal Brief 卡片  `[已实现]`

**实现位置**
- `packages/app/modules/agent-company/runtime/shared/goal-brief-view.ts`（新增）：纯函数。`goalBriefView` 投影结果导向的默认字段；`buildBriefAppendRequest` 局部编辑合并；`parseGoalBriefAppendResponse` 解析 append 响应。
- `packages/app/modules/agent-company/runtime/app/components/GoalBriefCard.vue`（新增）：默认五字段卡片 + “查看完整 Brief”折叠 + 局部编辑 + 开始/调整动作。
- `packages/app/modules/agent-company/runtime/server/api/goal-brief-append.post.ts`（新增）+ `module.ts` 路由：代理 Control Plane 的 `POST /goal-brief/:briefID/versions`。
- `packages/app/app/pages/inbox/index.vue`（改造）：用 `GoalBriefCard` 替换原来仅显示计数的占位摘要。
- `packages/app/modules/agent-company/runtime/app/assets/company-extension.css`（新增 `.ac-brief` 样式）。
- `packages/app/test/goal-brief-view.test.ts`（新增，12 用例）。

**设计要点**
- 默认卡片只展示：理解的目标、将交付、完成标准、推荐执行方式、需要决定（开放问题）。
- 低频字段（约束/非目标/假设/完整计划步骤/来源）折叠进“查看完整 Brief”。
- 局部编辑只重写受影响字段（goal/deliverables/acceptanceCriteria/constraints），其余逐字沿用当前版本；通过乐观并发（expectedVersion）提交，版本冲突会提示。
- 仅自主模式且无阻塞问题才显示“确认并开始”；开放问题为空时不渲染区块。

**开发者自测结论**：`bun run test:unit`（packages/app）→ 40 pass / 0 fail；`bun typecheck` clean。

### 待专人测试项
- [ ] E2E：在 Inbox 生成 Brief → 编辑任一字段 → 保存 → 刷新，确认修改不丢失且与后端版本一致。
- [ ] 一屏内可判断系统是否理解目标以及最终交付物（五个默认字段可见）。
- [ ] 启动普通目标的主要动作不超过 3 次。
- [ ] 内容测试：空字段、≥10 个交付物、超长约束、中英混排内容的渲染与编辑。
- [ ] 开放问题为空时不显示“需要决定”区块；有阻塞问题时不自动开始。
- [ ] 版本冲突：两个窗口先后编辑同一 Brief，后提交者收到“已被更新到第 N 版”提示不静默覆盖。
- [ ] 无障碍：键盘可编辑各字段，屏幕阅读器可读出字段名与变化（aria-live/label）。
- [ ] “查看完整 Brief”折叠展开/收起正常，低频字段齐全且不占主区。
- [ ] 服务不可用（Control Plane 断开）时编辑保存失败有明确提示，草稿不丢。

---

## R1 GOAL-05 — 删除强制大型 Charter 表单与 Markdown 解析回退  `[已实现]`

**实现位置**
- 删除死代码 `packages/app/modules/agent-company/runtime/app/pages/company/board.vue`（含 12+ 字段 Charter 立项表单、Markdown 标题解析器 `charterSection`、把目标文本复制进 title/value/scope/deliverables/acceptance 的 `prepareCharter`）。
- 移除仅供该表单使用的孤立样式 `.company-charter-card* / .company-project-decision`（`company-extension.css`，-92 行）。
- 新增静态守卫 `packages/app/test/goal-05-charter-removed.test.ts`（2 用例）。

**设计要点与现状**
- 真正路由的 `/company/board` 早已是重定向到 `/work` 的桩（`app/pages/company/board.vue`），带 Charter 表单的 `runtime/.../company/board.vue` 是未注册、无人引用的死代码；本任务将其彻底删除，使“旧 Markdown 解析器不再被生产路径引用”从“偶然未引用”变为“确定不存在”。
- 活跃主流程已全部改用结构化 Goal Brief：Inbox（`app/pages/inbox/index.vue`）生成并展示 `GoalBriefCard`；Work 详情（`app/pages/work/[projectID].vue`）展示结构化 Brief，旧项目走 `legacy_charter` 只读分支并显示“从旧项目 Charter 读取的只读目标摘要，缺失字段不会被推断或补写”的迁移提示。
- 不删除数据库历史 Charter 字段；`board-decide.post` 与 `board.get/post` 后端端点保留（e2e 直接测试其 API 契约，非 UI 表单）。

**开发者自测结论**：`bun run test:unit`（packages/app）→ 42 pass / 0 fail；`bun typecheck` → EXIT=0，无 TS 错误。

### 待专人测试项
- [ ] 静态测试：全代码库确认无 `charterSection`/`prepareCharter` 及 `**价值**/**范围**/**DRI**` 标题提取逻辑残留于生产路径。
- [ ] E2E · 纯文本模型回复：模型返回不含任何 Markdown 标题时，仍能生成结构化 Brief 并正常展示。
- [ ] E2E · 结构化输出：常规目标从输入到查看/确认全程不出现大型 Charter 立项表单。
- [ ] E2E · 历史 Charter：打开旧项目能显示只读目标摘要与迁移提示，且不阻塞新流程。
- [ ] 数据测试：新目标的字段来源相互独立，不再出现目标文本被同时复制为范围/交付物/验收标准。
- [ ] 回归测试：高级用户仍可查看完整治理信息（结构化 Brief 的完整字段 + 旧项目只读 Charter）。
- [ ] 视觉回归：删除 Charter 表单样式后，Board 相关页面/重定向无残留布局或样式错乱。

---

## R1 GOAL-04 — 重大歧义澄清与批准策略  `[已实现]`

**实现位置**
- 开放问题契约扩展：`packages/shared/src/experience.ts` 的 `GoalBriefOpenQuestion` 新增必填字段 `defaultAssumption`（若用户不回答时系统采用的默认假设）；现有 `impact` 承载“不同答案带来的影响”。模型生成（`generateObject`）自动受该 schema 约束。
- 纯逻辑模块：新增 `packages/app/modules/agent-company/runtime/shared/goal-brief-clarification.ts`（`disposeOpenQuestion`/`classifyOpenQuestions`/`resolveStartDecision`/`requiresExplicitGate` + `gatedActionKinds`）。
- 视图接线：`goal-brief-view.ts` 的 `goalBriefView` 新增 `materialQuestions`/`autoAdoptedAssumptions`/`hasMaterialQuestions`/`hasAutoAdoptedAssumptions`/`startReason`，`autoStart` 改由 `resolveStartDecision` 驱动。
- UI：`GoalBriefCard.vue` 将“需要决定”区改为只展示材料性问题（带“若不回答”默认假设），新增“系统已采用的默认假设”区。
- 单测：新增 `packages/app/test/goal-brief-clarification.test.ts`（10 用例）。

**设计要点与现状**
- 材料性规则（纯函数、可单测）：阻塞问题必问；高/严重风险目标下的开放问题也需用户确认；其余低风险可逆问题由系统采用 `defaultAssumption` 并展示，不逐项打断用户。
- 启动决策：仍有材料性问题时任何模式都不自动开始（`material_questions`）；无材料性问题时，自主=自动开始、平衡=展示 Brief 等待调整、严格=明确等待用户开始。
- 高风险动作 Gate：`requiresExplicitGate` 将 `external_write/publish/delete/payment` 标记为无论批准模式都必须显式 Gate 的不变量（产品规则）。
- **留待后端接线（本任务未虚构）**：（1）control-plane 尚未在 GoalBrief 运行时消费 `resolveStartDecision`/`requiresExplicitGate`（目前 approvalMode 仅在旧 charter `source_approval_preset` 路径参与 execution）；（2）`GateKind` 仍为 `risk_approval/merge_approval`，尚无 publish/delete/payment 细分门禁；（3）无 clarification/decision 专用审计 Kind；（4）跨 Agent 语义级歧义合并（同一缺口合并为单个 Attention Item）仍需后端实现。（5）SDK 生成产物 `packages/sdk/js/src/v2/gen/*` 未重生，仍为旧 openQuestion 形状（需跑 `./packages/sdk/js/script/build.ts` 重生）。

**开发者自测结论**：`bun run test:unit`（packages/app）→ 52 pass / 0 fail；`bun test test/goal-brief`（packages/control-plane）→ 35 pass / 0 fail；`bun typecheck`（app / control-plane / shared）均 EXIT=0。

### 待专人测试项
- [ ] 场景测试 · 低风险明确目标：无开放问题，自主模式直接自动开始，无无效打断。
- [ ] 场景测试 · 低风险歧义目标：非阻塞问题不弹窗询问，“系统已采用的默认假设”区正确展示 `defaultAssumption` 并可追溯。
- [ ] 场景测试 · 高风险外部动作：外部写入/发布/删除/支付在任何批准模式下都触发显式 Gate（待后端接线后验证端到端）。
- [ ] 模式矩阵测试：自主/平衡/严格三种模式下的启动（autoStart/startReason）与 Gate 行为符合预期。
- [ ] 重复问题测试：多个 Agent 发现同一缺口时合并为单个 Attention Item（依赖后端语义合并，当前仅按稳定 key 去重）。
- [ ] 审计测试：每个系统假设（自动采用的默认值）与用户决定均可追溯（依赖 clarification/decision 审计 Kind 接线）。
- [ ] 批准策略切换只影响未来动作，不会悔改已等待中的 Gate。
- [ ] 无障碍：材料性问题与默认假设区可被键盘遍历、屏幕阅读器可读出“若不回答”默认假设。
- [ ] 回归：旧项目（legacy_charter，openQuestions 为纯文本数组）仍能正常打开，不受 `defaultAssumption` 必填字段影响。

---

## R1 WORK-01 — 重建 Work 列表与项目进入路径  `[已实现]`

**实现位置**
- 纯逻辑模块：新增 `packages/app/modules/agent-company/runtime/shared/work-list.ts`（`workGroups` 语义四分组、`workGroupOfStatus`/`statusToGroup` 状态映射、`toWorkListEntry` 投影归一化、`matchesFilter`/`selectWork` 筛选排序、`groupCounts` 计数、`ownerOptions` 负责人去重）。
- UI：全量重写 `packages/app/app/pages/work/index.vue`（4 分组页签 + 计数、搜索框、负责人下拉、深链接 `route.query` 持久化、无结果空态 + 清除筛选，保留 available/unavailable 卡片渲染）。
- 样式：`packages/app/app/assets/css/main.css` 追加 `.ac-work-toolbar/.ac-work-tabs/.ac-work-tab/.ac-work-filters/.ac-work-search/.ac-work-owner/.ac-work-clear`。
- 单测：新增 `packages/app/test/work-list.test.ts`（9 用例）。

**设计要点与现状**
- 语义四分组（需要处理 / 正在执行 / 已交付 / 全部），不向用户暴露内部 13 态枚举；`cancelled` 归为 `other` 只在“全部”出现。
- 状态不可用（`availability=unavailable`）的工作归一化为“需要处理”并保留标题/时间，不静默丢弃。
- 深链接：分组/搜索/负责人筛选同步到 `route.query`，刷新与返回可恢复；空筛选不写入冗余 query。
- 排序：按 `updatedAt`（ISO 时间戳字典序=时间序）最近活动降序。
- **留待后端/流程接线（本任务未虚构）**：（1）WORK-01 规格中“项目创建后立即跳转到对应 Work 并明确告知已开始/等待什么”依赖正式提交/创建流程，该流程在产品层尚未开放（见 GOAL-05 留档），故创建后跳转路径未接线；（2）大数据量（约 1000 项）仅普通渲染，未做虚拟滚动，性能需专人验证。

**开发者自测结论**：`bun test test/work-list.test.ts`（packages/app）→ 9 pass；`bun run test:unit`（packages/app）→ 61 pass / 0 fail across 13 files；`bun typecheck`（packages/app）→ EXIT=0。

### 待专人测试项
- [ ] E2E：创建项目 → 跳转 Work（待流程接线后验证）→ 分组切换 → 搜索 → 刷新 → 返回，筛选状态保持一致。
- [ ] 数据规模：0 / 1 / 50 / 1000 项工作下的加载、分组计数、滚动与响应性能。
- [ ] 状态更新：一个工作从 Running → Needs you → Delivered，确认在分组间实时移动且计数同步。
- [ ] 内容测试：超长目标标题、超长负责人名不破坏卡片布局。
- [ ] 深链接/刷新：带 `?group=&q=&owner=` 的 URL 直接打开可正确还原筛选。
- [ ] 空态：无任何工作、有工作但无匹配筛选，两种空态文案与“清除筛选”按钮行为正确。
- [ ] 无障碍：键盘可遍历分组页签、搜索框、负责人下拉，屏幕阅读器可读出分组名与计数。
- [ ] 服务不可用（Control Plane 断开）时显示连接状态而非空列表误导。

---

## R1 TRUST-03 — 重做 Provider 配置为面向用户的连接向导  `[已实现]`

**实现位置**
- 纯逻辑模块：新增 `packages/app/modules/agent-company/runtime/shared/provider-wizard.ts`（`providerPresets` 六个预设入口、`providerPreset` 回退、`classifyProviderError` 错误分类、`buildModelsRequest`/`buildConfigureRequest` 请求体构造）。
- 服务端代理：新增 `runtime/server/api/provider-models.post.ts`（代理 `POST /company/providers/models` —— 真实连接测试 + 模型发现）；改造 `runtime/server/api/provider.put.ts`（代理 `PUT /company/provider`）——两者均透传后端错误名称/文案/状态，返回 `{ ok, ... }` 判别联合。已在 `module.ts` 注册新路由。
- UI：重写 `runtime/app/pages/settings/company.vue` 的模型服务区为向导（预设卡片 → 基础凭据（高级设置收纳端点/格式/服务标识/请求头）→ 测试连接 → 选择发现的模型 → 验证并保存）。
- 样式：`runtime/app/assets/company-extension.css` 追加 `.ac-provider-presets/.ac-provider-preset*/.ac-provider-actions/.ac-provider-models/.ac-provider-model*`。
- 单测：新增 `packages/app/test/provider-wizard.test.ts`（11 用例）。

**设计要点与现状**
- 预设入口覆盖 OpenAI / Anthropic / Codex / Claude Code / 本地模型 / 自定义；基础模式只要求必要凭据，端点/格式/服务标识/请求头收进“高级设置”。
- 保存前先调用模型发现（真实连接后端端点的 `/models`）作为连接测试，成功后展示可选模型列表供选择；绑定时 `PUT /company/provider` 会再次校验模型存在并写入配置。
- 错误区分：凭据无效（401/403）、端点不可达（无法连接/404/5xx）、配额限制（429）、模型不存在（CompanyModelNotAvailable）、能力不兼容（模型列表格式无效）。
- 密钥只写入本机，保存后不回显明文（密码输入框、保存后清空、从不预填）；当前已配置状态通过快照 `company.provider` 展示。
- **留待后端接线（本任务未虚构）**：（1）逐模型的工具调用/结构化输出/中断恢复能力检测：后端模型发现端点仅返回 `{model_id, name}`，configureProvider 将 `tool_call` 硬编码为 true，尚无真实能力探测，因此向导不展示未经验证的能力开关；（2）本地模型：模型发现允许无密钥，但后端绑定（CompanyProviderConfigureInput）仍要求 api_key 必填，故本地预设仍需填占位密钥；（3）OAuth 类 Provider（如需浏览器授权的服务）不在本向导范围，仍走现有 OAuth 端点。

**开发者自测结论**：`bun test test/provider-wizard.test.ts`（packages/app）→ 11 pass；`bun run test:unit`（packages/app）→ 72 pass / 0 fail across 14 files；`bun typecheck`（packages/app）→ EXIT=0。

### 待专人测试项
- [ ] 集成测试：有效 Key、无效 Key、错误 Endpoint、模型不存在、429、超时，每种都给出区分清楚的错误提示。
- [ ] 可用性测试：5 名目标用户在无外部文档帮助下完成至少一种主流 Provider 配置，成功率 ≥ 80%。
- [ ] 有效配置保存后显示连接成功与绑定模型；无效配置不会被保存为默认可用状态。
- [ ] API Key 保存后不回显明文，重新编辑时为明确的“已配置”状态（当前服务可见、密钥框不预填）。
- [ ] 自定义 Provider 仍可通过高级模式完整配置（接口格式/端点/服务标识/请求头）。
- [ ] 本地模型：对 Ollama/LM Studio 等本机 OpenAI 兼容服务可发现模型并绑定（含占位密钥）。
- [ ] 能力提示：不支持工具调用或结构化输出的模型给出明确限制（依赖后端能力探测接线后验证）。
- [ ] 回归测试：已有 Provider 配置可迁移并继续使用。
- [ ] 无障碍：预设卡片（radiogroup）、密钥/端点输入、模型单选均可键盘遍历，错误以 role=alert 播报。

---

## R1 TRUST-04 — 建立显式 Demo Workspace 与首次目标引导  `[已实现]`

**实现位置**
- 纯逻辑模块：新增 `packages/app/modules/agent-company/runtime/shared/onboarding.ts`（本地引导状态 `parseOnboardingState`/`serializeOnboardingState`、阶段判定 `onboardingStage`、首次真实路径 `shouldEnterFirstGoal`、状态转换 `chooseReal`/`chooseDemo`/`skipOnboarding`/`exitDemo`/`restartOnboarding`/`markFirstGoalSubmitted`、与真实数据隔离的 `demoScenario`）。
- 组件：新增 `runtime/app/components/OnboardingChoice.vue`（首次两个清晰选项 + 跳过）、`runtime/app/components/DemoWorkspace.vue`（不可忽略的演示标识 + 脚本化示例 + 连接真实/退出/重置）。已在 `module.ts` 注册两个组件。
- 路由页：新增 `runtime/app/pages/welcome/company.vue`，`module.ts` 通过 `extendPages` 注册 `/welcome`；无需引导时替换式跳回 `/inbox`。
- 接入：改造 `packages/app/app/pages/inbox/index.vue`（首次进入且未选择时以两个选项替代直接进入目标草稿）；改造 `runtime/app/pages/settings/company.vue`（Provider 保存成功后按 `shouldEnterFirstGoal` 直接进入目标输入；新增“引导与演示”区提供重新开始引导/进入演示）。
- 样式：`runtime/app/assets/company-extension.css` 追加 `.ac-onboarding*`、`.ac-onboarding-controls`、`.ac-demo*`（含移动端单列回退）。
- 单测：新增 `packages/app/test/onboarding.test.ts`（13 用例）。

**设计要点与现状**
- 首次进入提供“连接真实工作区”与“查看演示”两个清晰选项，并可跳过；进入演示前明确说明数据与执行均为示例。
- Demo 全程显示不可忽略的演示横幅（sticky、无关闭按钮），数据来自本地静态 `demoScenario()`，**从不经由真实快照/Provider/项目接口**，与真实数据库、Provider、项目完全隔离；不调用真实付费模型（符合“不在范围”）。
- 首次真实路径：Provider 配置完成后据 `shouldEnterFirstGoal` 直接跳到 Inbox 目标输入，而非停留在设置或功能菜单。
- 空工作区不展示统计卡片，只呈现单一清晰下一步（两个选项 / 目标草稿）。
- 允许跳过、退出演示、重置演示（重载初始场景）、重新开始引导（清除本机演示与引导标记，不影响真实数据）。
- **留待后端接线（本任务未虚构）**：（1）真正的隔离 Demo 数据库/租户在 control-plane 尚不存在；当前演示为客户端明确标注的脚本化示例（未写入真实库），若未来需要“演示态运行真实 Agent”，需后端提供隔离数据层（可复用 `AGENTCOMPANY_DB` 切库能力）；（2）引导状态仅存于浏览器 localStorage，未与后端 `company_setup_goal` 打通；（3）e2e 红线：不得通过 `?fixture=1&demo=1` 之类查询参数开启演示回退，本实现的演示是独立客户端路由与本地状态，未触及快照回退路径。

**开发者自测结论**：`bun run test:unit`（packages/app）→ 85 pass / 0 fail across 15 files；`bun typecheck`（packages/app）→ EXIT=0，无 TS 错误。

### 待专人测试项
- [ ] 首次进入：全新浏览器（无 localStorage）打开 `/inbox`，确认出现“连接真实工作区 / 查看演示”两个清晰选项且可跳过。
- [ ] 认知测试：进入演示前用户明确知道数据与执行均为演示；进入后能随时准确说出“当前处于演示”。
- [ ] 隔离测试：演示中的员工/工作/制品不出现在真实工作区；演示不产生任何真实项目、Provider 绑定或数据库写入。
- [ ] 切换测试：演示 ↔ 真实工作区来回切换后，无数据串用、状态串用或误提交。
- [ ] 首次真实路径：在设置页完成 Provider 连接后（首次、无既有工作）自动进入 Inbox 目标输入，而非停留设置页。
- [ ] 空工作区：真实连接但无工作时不展示统计卡片，只呈现单一清晰下一步。
- [ ] E2E：首次进入、跳过、重置演示、退出演示、重新开始引导、再次进入演示全流程可用且状态正确持久化。
- [ ] 删除演示/引导数据：`设置 → 引导与演示 → 重新开始引导` 后本机演示与引导标记被清除且不影响真实数据。
- [ ] 演示横幅在滚动过程中始终可见且无法被关闭。
- [ ] 无障碍：两个选项与跳过、演示内的进度条与操作按钮均可键盘遍历，演示标识对屏幕阅读器可读（role=note）。
- [ ] 回归：已有真实工作/历史数据的用户打开 `/inbox` 不会被欢迎选择或演示打断（直接进入常规界面）。

---

## R1 SHELL-04 + FND-04 — 响应式布局 / 视觉层级 / 可读性 / 设计系统基线  `[部分实现·以视觉验收为主]`

**实现位置**
- 设计 token：`packages/app/app/assets/css/main.css` `:root` 追加类型标尺与可读下限 token（`--ac-text-min: 11px`、`--ac-text-caption`、`--ac-text-body`、`--ac-text-lead`），并以注释固化五级视觉层级映射。
- 长文本保护：`main.css` 末尾新增规则，对 `.ac-workspace-title`、`.ac-card-heading h2`、`.ac-detail-heading h2`、`.ac-team-card h2`、`.ac-inline-item h3`、`.ac-brief-goal`、`.ac-brief-item`、`.ac-goal-generation-state__goal`、`.ac-card-reason`、`.ac-card-impact`、`.ac-artifact-link strong/small` 施加 `overflow-wrap: anywhere; word-break: break-word;`。
- 响应式：`main.css` 新增 `@media (max-width: 1120px)` 中间宽度内边距收敛（工作区页与设置页 `padding-inline: 32px`）；既有 `@media (max-width: 720px)` 提供窄屏单列回退。

**设计要点与现状**
- 五级层级：L1 主标题 `clamp(26px,3vw,38px)` · L2 区块标题 16-17px · L3 主要内容 15px · L4 正文 13px · L5 元信息 11-12px；已在 token 注释中固化。
- 可读下限：关键状态由 11px 起（如 `.ac-status-badge`）；审计确认 10px 仅用于全大写装饰性 eyebrow/kicker/meta，不承载关键运行状态，符合“禁止 9-10px 承载关键状态”。
- 同类卡片共享结构：`.ac-work-card`/`.ac-attention-card`/`.ac-library-card`/`.ac-team-card`/`.ac-detail-panel` 共用边框、圆角、阴影与内边距规范（既有）。
- 焦点/禁用/悬停/加载/空/错误：焦点统一 `outline: 2px solid var(--ac-focus)`；禁用态 `[aria-disabled]` 弱化；`@media (hover: hover)` 独立悬停；空/错误/加载态各有专用类（既有）。
- **留待后端/后续任务（本任务未虚构）**：（1）FND-04/SHELL-04 定义的“三栏工作区（左任务列表 / 中工作流 / 右上下文）”属 R2 WORK-02 范畴，本次仅补齐设计系统基线与单列页面的响应式/长文本/层级，尚未落地三栏容器与各栏独立滚动边界；（2）组件状态矩阵（每个核心组件的默认/悬停/焦点/禁用/加载/错误/空/长文本样例）尚无自动化视觉快照，需人工评审或引入视觉回归工具。

**开发者自测结论**：本任务为 CSS/设计系统改动，无纯逻辑单测；`bun run test:unit`（packages/app）保持 85 pass / 0 fail；`bun typecheck` 不覆盖 CSS。功能性验收需依赖下列人工/视觉测试。

### 待专人测试项
- [ ] 视觉回归：1440 / 1280 / 1024 / 768 四种宽度下，空 / 正常 / 错误 / 超长内容四种状态截图比对，无横向溢出、无不可达操作。
- [ ] 扫描测试：5 名用户在任一 Work/Inbox 页面 5 秒内指出当前目标、状态与下一步，成功率 ≥ 80%。
- [ ] 缩放测试：浏览器 200% 缩放下可完成目标提交 / 审批 / 交付验收，不依赖鼠标悬停显示关键信息。
- [ ] 长文本测试：超长中文、英文、无空格路径、超长 Agent 名分别注入标题/正文/制品名，验证换行与截断规则，布局不破坏。
- [ ] 长列表：100+ 项列表下滚动流畅、无布局崩坏、卡片结构一致。
- [ ] 组件矩阵评审：逐项检查 Button / Status Badge / Attention Card / Goal Brief / Artifact / Gate / Delivery / Empty / Error / Loading 的默认/悬停/焦点/禁用/加载/错误/空/长文本状态，无未定义状态。
- [ ] 可读下限复核：确认所有承载关键运行状态的文字 ≥ 11px，10px 仅出现在装饰性标签。
- [ ] 无障碍：全键盘遍历一级导航与页面主要操作，焦点样式清晰可见（`outline` 2px）。

---

## R2 WORK-02 — 三栏高信号工作区（左选工作 / 中高信号 / 右上下文面板）  `[已实现·含后端缺口标注]`

**实现位置**
- 纯逻辑：`packages/app/modules/agent-company/runtime/shared/work-workspace.ts` — 右侧上下文面板派生（`availableContextPanels`）、活动面板解析（`resolveActivePanel`）、窄屏列切换（`nextColumn`/`prevColumn`）、按 projectID 隔离的视图状态（`viewStateFor`）与切项目防残留校正（`reconcileViewState`）。
- 页面：`packages/app/app/pages/work/[projectID].vue` — 左栏工作列表（snapshot.work，高亮当前）/ 中栏高信号（进展、工作项与决定、需处理、交付）/ 右栏 tab 上下文面板（目标/审批/制品/成员/诊断）；`useState("work-workspace-view")` 按 projectID 保存视图状态，`watch` + `reconcileViewState` 切项目时校正。
- 样式：`packages/app/app/assets/css/main.css` — `.ac-work3*`（三栏 grid `264px minmax(0,1fr) 344px`、各栏独立滚动、`@media (max-width: 960px)` 下由 `[data-column]` 驱动 list→main→context 单栏切换、tab 与列表高亮样式）。
- 数据源：右栏消费 `/api/agent-company/projects/:projectID`（`project.get.ts` 返回真实 `CompanyProjectDetail`：workItems/gates/artifacts/recruitment.candidates）与 goal-brief；面板仅在真实数据存在时才出现，空数据不制造面板。

**开发者自测结论**：`work-workspace.ts` 新增 13 个纯逻辑单测；`bun run test:unit`（packages/app）→ **98 pass / 0 fail across 16 files**；`cd packages/app && bun typecheck` → **EXIT=0，无 TS 错误**。UI 交互与响应式需依赖下列人工/E2E 测试。

**本任务未虚构的后端缺口（如实标注，归后续任务）**
- Thread 明细面板：前端无 thread messages 代理，`threadAvailable` 硬编码 `false`，Thread 面板不显示（属 WORK-03/04）。
- Composer：本页未内置消息发送 Composer（属 WORK-04；后端 POST `/company/channels/:channelID/messages` 已存在但前端 mentions 代理仍硬编码空数组）。
- 运行控制：本页未接 cancel/retry 按钮（属 WORK-07；pause / adjust-direction 后端尚不存在）。
- Gate 交互：右栏审批面板仅展示 Gate 状态，未接 approve/reject 操作（属 DELIV-04；request-changes 后端尚不存在）。

### 待专人测试项
- [ ] 多项目切换隔离：在 A/B 两个真实项目间来回切换，验证右栏选中的制品/成员/活动面板不会残留上一项目上下文（不存在的选中项被丢弃，面板回退到首个可用）。
- [ ] 状态保持：在项目 A 选定某制品/成员 → 切到 B → 再切回 A，验证之前的活动面板与滚动位置保留。
- [ ] 响应式：1440 / 1280 / 1024 三栏并存；≤960 切为单栏，移动列切换栏（列表/上下文按钮）可在 list→main→context 间切换且到边界停止。
- [ ] 各栏独立滚动：中栏长内容滚动时左栏工作列表与右栏 tab 不跟随滚动；右栏 tab 栏 sticky 置顶。
- [ ] 空/缺数据：无 gates/artifacts/agents/诊断 时对应 tab 不出现；全部缺失时右栏显“暂无可显示的上下文”而非空白或占位。
- [ ] 错误态：goal-brief / project detail 读取失败时对应面板显错误提示与重读按钮，不以工作标题猜测内容。
- [ ] 长会话：工作列表 100+ 项、制品/成员各 100+ 项时，滚动与切换仍流畅，无布局崩坏。
- [ ] 后端缺口确认：确认 Thread 明细 / Composer / 运行控制按钮 / Gate approve-reject 当前均未在本页提供，且页面未以空面板或假按钮假装已实现（归 WORK-03/04/07 与 DELIV-04）。

---

## R2 WORK-03 — 主会话降噪：高信号 / Thread / 诊断分离  `[逻辑完成·渲染受阻]`

**实现位置**
- 纯逻辑：`packages/app/modules/agent-company/runtime/shared/work-messages.ts` — 消息展示层分类：`classifyMessage`（带 threadID→Thread；非用户消息且命中内部运行噪声→Diagnostics；其余→高信号）、`isInternalNoise`（queued/projecting/winner selected/bidding/attempt N/heartbeat/tool_call/raw event 等运行态噪声正则）、`classifyMessages`（分离 highSignal / threads（按 threadID 分组保留首次出现顺序）/ diagnostics）、`hasThreads`。仅做展示层归类，不删除任何原始消息。

**开发者自测结论**：`work-messages.ts` 新增 11 个纯逻辑单测；`bun run test:unit`（packages/app）→ **115 pass / 0 fail across 18 files**（含 WORK-03 11 项 + WORK-07 6 项）；`cd packages/app && bun typecheck` → **EXIT=0，无 TS 错误**。

**渲染集成受阻（如实标注，未虚构）**
- 契约中的 `CompanyMessage` 为**公司全局消息**（`{id,author,role,body,time,kind,threadID?}`，**无 projectID**），无法在当前投影层归属到单个工作/项目。
- 新体验壳目前**没有群聊主会话界面**：`packages/app/.../pages/company/board.vue` 已重定向到 `/work`。因此分类逻辑虽已就绪，但缺少可挂载的主会话渲染面板。
- 结论：本任务交付**分类纯逻辑 + 单测**，**未虚构 per-work 消息流**，也未在无群聊界面处强行渲染。渲染集成需前置能力（项目级消息归属，或恢复/新建群聊主会话界面）后再接线。

### 待专人测试项
- [ ] 分类正确性：用户消息即使含噪声词仍留在高信号；里程碑/交付/目标理解类留高信号；运行态（排队/投影/竞标/心跳/工具调用/原始事件）归诊断。
- [ ] Thread 优先级：带 threadID 的消息（即使命中噪声词）归 Thread，不误入诊断。
- [ ] Thread 分组：同一 threadID 的多条消息聚合为一组，且组顺序按各 thread 首次出现顺序稳定。
- [ ] 前置能力落地后回归：一旦项目级消息或群聊界面接入，验证主会话默认只显高信号、诊断可折叠展开、Thread 可进入明细，且原始消息零丢失。

---

## R2 WORK-04 — 重做 Composer 为统一意图与干预入口  `[已实现·附件与变更类干预受后端/契约约束]`

**实现位置**
- 纯逻辑：`packages/app/modules/agent-company/runtime/shared/company-composer.ts` — 发送目标（`ComposerTarget`：公司看板 / 当前项目）与目标标签、意图提示、`mentionOptions`（公司真实名册按 id 去重）、`toggleMention`（后端上限 20）、`shouldRotateRequestID`（仅服务端确认接受后轮换 request_id）、`draftStorageKey`（草稿按目标隔离）、`composerQuickIntents`（调整方向/添加约束/请求总结文本前缀）、`sendFailureText`（按真实状态码给出失败文案并强调内容已保留）。
- 代理：`packages/app/modules/agent-company/runtime/server/api/message.post.ts` — `POST /api/agent-company/messages`：按目标解析 board / project 频道（`kind==="project" && scopeID===project_id`），透传真实 `request_id`/`body`/`mentions` 到 Control Plane `POST /company/channels/:channelID/messages`；上游拒绝时按原状态码如实抛错，不降级为假成功。
- 组件：`packages/app/modules/agent-company/runtime/app/components/CompanyComposer.vue` — 发送对象与意图提示常显；@Agent 选择器（真实名册，选中汇总计数）；快捷意图前缀按钮；草稿按目标存 localStorage（项目切换/断线/刷新后恢复）；同一草稿沿用同一 request_id（双击/重试由 intake 幂等去重），仅发送成功后轮换并清空；失败保留全部内容并 `role="alert"` 如实显示原因；成功后 `role="status"` 显示发送时间确认；⌘/Ctrl+Enter 发送。
- 集成：`packages/app/app/pages/work/[projectID].vue` — 项目可用（availability===available）时在主栏底部挂载，目标固定为当前项目频道，发送成功后刷新快照。
- 样式：`packages/app/modules/agent-company/runtime/app/assets/company-extension.css` — `.ac-composer*`。

**开发者自测结论**：`company-composer.ts` 新增 14 个纯逻辑单测；全量 `bun run test:unit`（packages/app）→ **173 pass / 0 fail across 25 files**；`cd packages/app && bun typecheck` → **EXIT=0**。

**本任务未虚构的边界（如实标注）**
- 附件/目录引用：后端无对应端点，Composer 不提供附件入口，不以假上传控件假装支持。
- 暂停/停止等变更类干预：属 WORK-07 运行控制条，受 `ExperienceR0ImplementedMutationActions = []` 契约门禁恒禁用；Composer 仅提供文本快捷意图前缀，不伪造命令。
- mentions 可见性：由本地服务 `mentionsAreVisible` 校验，前端不预判；不可见时发送明确失败并保留内容（组件内已提示）。
- 发送后的消息列表呈现：work 页尚无项目级消息流渲染（WORK-03 既有渲染受阻），即时反馈由组件内发送确认文案提供，不伪造消息回显。
- board 目标：纯逻辑与代理已支持 board 频道，但新体验壳目前没有看板消息流界面（Inbox 为注意力队列，新目标走 GOAL-01 目标草稿入口），board Composer 未挂载，待看板界面恢复后接线。

### 待专人测试项
- [ ] 发送对象明确：在项目工作区发送前可一眼看到“发送到：当前项目 · X”与默认意图提示（不会创建新项目）。
- [ ] 真实送达：发送后在 Control Plane 项目频道中能查到该消息（含 mentions），而非仅前端显示成功。
- [ ] 草稿恢复：输入未发 → 切换项目/断开本地服务/刷新页面 → 返回后草稿仍在且按目标隔离（A 项目草稿不会出现在 B）。
- [ ] 幂等：快速双击发送 / 发送失败后重试，后端只落一条消息（同 request_id 被 intake 去重）；发送成功后再发新内容为新消息。
- [ ] mentions 失败路径：提及不在目标频道的 Agent 时发送明确失败（403 文案）且内容与已选 mentions 保留。
- [ ] 失败不丢内容：停掉本地服务后发送，错误文案如实显示且草稿/提及不丢；恢复后重试成功。
- [ ] 键盘与可访问性：⌘/Ctrl+Enter 发送；错误 `role="alert"`、成功 `role="status"` 读屏可感知；名册为空时 @ 按钮禁用并有提示。
- [ ] 边界确认：页面无附件假控件、无暂停/停止假按钮；快捷意图仅为文本前缀。

---

## R2 WORK-06 — SSE/事件流替代高频轮询 + 可靠重连  `[已实现·无 Last-Event-ID 补发，以全量快照校准替代事件回放]`

**实现位置**
- 纯逻辑：`packages/app/modules/agent-company/runtime/shared/company-events.ts` — `classifyGlobalEvent`（server.connected→connected / server.heartbeat→heartbeat / 其余业务事件→signal / 畸形不抛异常→unknown）、`nextSignalRefreshDelay`（突发事件合并，最小间隔 1s 节流）、`streamStalled`（超 3 个心跳周期无事件视为停滞）。
- 代理：`packages/app/modules/agent-company/runtime/server/api/events.get.ts` — `GET /api/agent-company/events`：原样转发 Control Plane `GET /event`（text/event-stream）；浏览器断开时 abort 上游连接；上游不可用按真实状态码抛错（EventSource 自动重试），不提供假心跳、不伪造 server.connected。same-origin 门禁对 GET 豁免，EventSource 可直连；session 鉴权仍生效。
- 集成：`packages/app/modules/agent-company/runtime/app/composables/useCompanySnapshot.ts` — 模块级单例 EventSource（引用计数管理生命周期）；connected/signal 事件 → 节流后的静默全量快照刷新（不改写连接态，避免每次事件闪现 recovering）；heartbeat 仅保活不驱动刷新；同一标签页多组件共享一条订阅且只触发一个实例刷新；SSE 不可用/中断时降级回既有断线重连轮询 + 手动刷新，不静默停更。

**开发者自测结论**：`company-events.ts` 新增 8 个纯逻辑单测；全量 `bun run test:unit`（packages/app）→ **173 pass / 0 fail across 25 files**；`cd packages/app && bun typecheck` → **EXIT=0**。

**本任务未虚构的边界（如实标注）**
- 后端 `GET /event` **不提供事件 ID / Last-Event-ID 补发**：断线期间的事件无法回放，前端不伪造游标去重，重连后以一次全量快照校准状态（server.connected 触发），因此“不丢”由快照全量保证、“不重”由只读刷新与 request_id 幂等保证，而非事件级回放。
- 旧轮询移除：新体验壳本就无高频轮询（仅断线重连退避轮询），本任务保留该重连机制作为 SSE 不可用时的如实降级，未引入新轮询。
- p95≤0.5s 等量化验收指标未自测断言，归下列专人测试。

### 待专人测试项
- [ ] 实时性：Agent 产生新消息/状态变化后，界面在 p95≤0.5s 内反映（事件触发 + 1s 节流上限，需实测分布）。
- [ ] 断线恢复：断网/停本地服务 30s 后恢复，EventSource 自动重连，server.connected 触发全量校准，界面状态不丢不重（无重复条目、无停更）。
- [ ] 多标签页：同时打开 2+ 标签页，各自订阅只触发只读刷新；在一页发送消息，另一页自动更新且不重复执行任何业务动作。
- [ ] 浏览器休眠：标签页后台挂起/系统休眠后唤醒，SSE 重建或降级轮询接管，界面最终收敛到最新状态。
- [ ] 事件风暴：短时间大量事件（如多 Agent 并发）时刷新被 1s 节流合并，无请求风暴、无 UI 卡顿。
- [ ] 降级路径：阻断 `/api/agent-company/events`（保留 snapshot），页面仍可手动刷新与断线重连，不静默停更也不假装实时。
- [ ] 边界确认：断线期间产生的事件不会逐条回放（后端无 Last-Event-ID），但重连后全量快照已包含其结果；确认心跳不触发刷新。

---

## R2 WORK-07 — 运行控制动作条  `[前端如实渲染·变更类受契约门禁]`

**实现位置**
- 纯逻辑：`packages/app/modules/agent-company/runtime/shared/work-controls.ts` — `actionLabels`（20 个动作类型中文标签）、`clientHandlerFor`（导航/查看类 view_progress/open_diagnostics/open_delivery/view_evidence 与 retry 映射到客户端处理器，其余变更类→none）、`toControlActions`（保留投影下发的真实 enabled/disabledReason，并按 `ExperienceActionMutatesBusinessState` 标注是否变更业务状态）、`canInvoke`（仅 enabled 且客户端有处理器才可点击）。
- 页面：`packages/app/app/pages/work/[projectID].vue` — header 后运行控制动作条（`v-for controlActions`），按投影 `summary.allowedActions` 如实渲染；`summary.nextAction` 高亮为主动作；禁用态 `:disabled` + `:title=disabledReason` + `:aria-disabled`；`invokeAction`：导航/查看类在客户端切列/切面板，retry 走真实代理 `POST /api/agent-company/projects/:id/retry`（`retrying` ref 防重复提交，成功后 `refresh` + `refreshGoalBrief`）。
- 样式：`packages/app/app/assets/css/main.css` — `.ac-work3__actions`（flex wrap）+ `.ac-work3__action`（主动作高亮 `[data-primary="true"]`，禁用态虚线弱化 `not-allowed`）。

**开发者自测结论**：`work-controls.ts` 新增 6 个纯逻辑单测；全量 `bun run test:unit` → **115 pass / 0 fail**；`bun typecheck` → **EXIT=0**。

**契约驱动的诚实渲染（如实标注，未虚构）**
- 契约 `packages/shared/src/experience.ts` 中 `ExperienceR0ImplementedMutationActions = []`（空），`ExperienceActionDescriptor` 的 superRefine 拒绝任何 `enabled:true` 的变更类动作。因此暂停/停止/调整方向/批准/驳回/请求修改等**变更类动作在 R0 解除前恒为 disabled**，动作条如实显示禁用与原因，绝不伪装可用。
- retry 属已有真实 app 侧代理（`project-retry.post.ts`，代理模式取 repository.root_path 作 directory），可真实调用；cancel / gate-resolve 的 app 侧代理尚未建立；pause / adjust-direction / request-changes 后端不存在。

### 待专人测试项
- [ ] 状态矩阵：对每个 userStatus（drafting/working/blocked/awaiting_review/delivered/failed 等），验证动作条展示的动作集合、enabled/disabled 与投影 allowedActions 完全一致，无多余或缺失按钮。
- [ ] 主动作高亮：`nextAction` 对应按钮高亮为主动作，且仅高亮一个。
- [ ] 禁用原因可见：disabled 动作悬停 `title` 与 `aria-disabled` 展示投影下发的 disabledReason，读屏可感知。
- [ ] retry 幂等：连续双击 retry 只发一次请求（`retrying` 期间按钮禁用），完成后刷新工作与目标简报。
- [ ] 导航动作跳转：view_progress/open_delivery 切到中栏主流、open_diagnostics 切诊断面板、view_evidence 切目标面板，均在客户端内完成不误发请求。
- [ ] 切项目不残留：从项目 A 切到 B，动作条按 B 的 allowedActions 重建，不残留 A 的动作或 `retrying` 态。
- [ ] 契约门禁回归：确认变更类动作在 R0 未解除时全部 disabled；一旦 `ExperienceR0ImplementedMutationActions` 放开，验证对应动作转为可点并正确接线（届时需补 cancel/gate-resolve 代理）。

---

## R2 DELIV-01 — Inbox / Attention Center 分类聚合  `[已实现·含后端缺口标注]`

**实现位置**
- 纯逻辑：`packages/app/modules/agent-company/runtime/shared/inbox-attention.ts` — `aggregateAttention`（只聚合可用工作的 attentionItems、按同 id 去重、先优先级后时间倒序排序）、`countByType`（input/approval/blocked/delivery/failure 五类计数 + total）、`categorySummaries`（只列 count>0 的类别）。
- 页面：`packages/app/app/pages/inbox/index.vue` — 用 `aggregateAttention` 替换原内联聚合/排序逻辑；新增分类计数概览条（总计 + 五类 chip）；事项深链到 `/work/:workId`；空状态引导创建目标/查看 Work。
- 样式：`packages/app/app/assets/css/main.css` — `.ac-attention-summary*`（总计 + 分类 chip，按类型轻量色彩区分）。

**开发者自测结论**：`inbox-attention.ts` 新增 6 个纯逻辑单测；`bun run test:unit` → **130 pass / 0 fail across 20 files**；`bun typecheck` → **EXIT=0**。

**后端缺口（如实标注）**：“不处理的后果”文案依赖投影下发（当前 AttentionItem 只有 reason/impact，无独立 consequence 字段）；处理后移出与历史追溯依赖变更类动作接线（受 WORK-07 契约门禁）。

### 待专人测试项
- [ ] 默认首页进入 Inbox；空状态明确引导创建目标或查看正在运行工作。
- [ ] 聚合去重：同一根因（相同事项 id）不会产生多个重复 Attention Item。
- [ ] 排序：高风险审批（critical/approval）优先于普通交付提示，同级按更新时间倒序。
- [ ] 分类计数一致：概览条总计与列表实际数量一致；count 为 0 的类别不展示。
- [ ] 深链：点击事项可进入正确项目/工作（待处理于对应 Work 页）。
- [ ] 并发：在其他标签页处理事项后，当前页刷新后同步移出（依赖快照刷新）。

---

## R2 DELIV-02 — Project 页重构为目标与里程碑工作台  `[诚实修复·去除原始字段泄露]`

**实现位置**：`packages/app/app/pages/work/[projectID].vue` 中栏工作台。
- 首屏元信息：头部新增 `.ac-work-meta` 展示**负责人（summary.owner）/ 下一里程碑（summary.nextMilestone）/ 是否需用户行动（summary.needsUserAction）**，直接回答“谁负责 / 下一步”。
- 去除原始字段泄露：工作项状态用 `workItemStatusLabel` 映射为中文标签（未知值原样显示）；**默认视图移除 attempt/maxAttempts 尝试次数**（属内部诊断）；Gate 状态用 `gateStatusLabel` 映射。

**开发者自测结论**：`bun typecheck` → **EXIT=0**；`bun run test:unit` → **130 pass / 0 fail**（本项为 UI 改动，行为需下列人工测试）。

**尚存限制**：契约仅提供单个 `nextMilestone`，无完整里程碑列表；`detail.workItems` 仍为内部分解，本次只做状态可读化与隐藏尝试次数，未将其彻底折叠进诊断。

### 待专人测试项
- [ ] 场景测试：新建/运行/阻塞/审批/复核/交付/失败/取消各状态下，首屏均能说明目标/阶段/负责人/下一里程碑/是否需行动。
- [ ] 无原始字段：默认视图不出现 raw status 枚举、attempt/maxAttempts、raw ID、rejected candidate。
- [ ] 一致性：Work 卡片 / Inbox / Project 页状态一致，不出现项目显示完成而任务仍运行。
- [ ] 可用性：用户 10 秒内能指出下一步与阻塞。
- [ ] 未知状态容错：后端下发未映射的 status 字符串时原样展示，不报错不猜测。

---

## R2 DELIV-04 — Gate 批准/拒绝/请求修改交互  `[前端决策面就绪·变更类受契约门禁]`

**实现位置**：`packages/app/app/pages/work/[projectID].vue` 右栏审批面板。
- Gate 状态可读化（`gateStatusLabel`）；新增 `.ac-gate-decision` 决策面：决策说明 textarea + 从投影 `allowedActions` 筛出的批准/拒绝/请求修改动作（`decisionActions`，复用 work-controls.toControlActions），按真实 enabled/disabledReason 展示。
- 复用 `canInvoke`/`invokeAction`：变更类 handler 为 none 且契约下 enabled=false，故按钮禁用且点击无副作用。

**开发者自测结论**：`bun typecheck` → **EXIT=0**；`bun run test:unit` → **130 pass / 0 fail**。

**后端缺口（如实标注）**：契约 `ExperienceR0ImplementedMutationActions=[]` 使 approve/reject/request_change 恒为 disabled；app 侧**尚无** gate-resolve 代理（后端 `/company-project/:id/gates/:gateID/resolve` 存在但仅 approve/reject，request_change 后端不存在）；决策说明文本在动作可用前不提交。Gate 级 allowedActions 尚未在投影中按 gate 粒度下发（当前为工作级）。

### 待专人测试项
- [ ] 可达性：任何 Needs approval 状态均能从 Inbox 与 Project 到达可见的 Gate 决策面。
- [ ] 契约门禁：R0 未解除时批准/拒绝/请求修改均 disabled 且悬停显示原因；无伪可提交按钮。
- [ ] 解除后回归（需后端）：契约放开 + 新增 gate-resolve 代理后，验证批准/拒绝驱动真实业务结果、幂等（双击/重试只一次）、过期/已处理 Gate 阻止陈旧提交、可追溯到用户/时间/理由/证据。
- [ ] request_change 缺口：确认后端无 request_change 时前端不提供可提交入口。

---

## R2 QA-06 — 本地优先的产品遥测与体验指标  `[纯逻辑核心完成·采集与仪表盘待接线]`

**实现位置**：`packages/app/modules/agent-company/runtime/shared/product-telemetry.ts`（纯逻辑）。
- 事件定义：`productEventTypes`（goal_created / brief_ready / execution_started / attention_requested / blocked / delivery_viewed / accepted / revision_requested / failed / recovered）。
- 隐私脱敏：`isForbiddenKey` + `sanitizeEventProps` + `isSafeEvent` — 禁采 API Key / Prompt / 消息 / 文件 / Artifact 正文 / 令牌密钥等键名。
- 去重与口径：`dedupeEvents`（同 type+dedupeKey 只记一次）、`computeMetrics`（北极星 deliveryReachRate + 护栏 interruptionRate/recoveryRate/acceptanceRate，带 `metricDefinitionVersion` 口径版本）。
- opt-in：`parseConsent`/`serializeConsent`/`shouldReportRemote` — 默认关闭远程上报，显式加入才上报。

**开发者自测结论**：`product-telemetry.ts` 新增 9 个纯逻辑单测；`bun run test:unit` → **130 pass / 0 fail**；`bun typecheck` → **EXIT=0**。

**尚未接线（如实标注）**：本任务交付遥测**纯逻辑核心 + 单测**；**尚未**在各业务流程埋点发事件、尚未接本地存储/导出/清除、尚未建仪表盘与可选匿名上报通道（归后续接线，需 FND-01 事件总线）。

### 待专人测试项
- [ ] 隐私：扫描事件 payload 不含 API Key / 完整 Prompt / 文件内容 / Artifact 正文（sanitizeEventProps 已剔除敏感键）。
- [ ] 事件完整性（接线后）：核心 E2E 产生预期事件且同一根因只记一次（dedupeKey）。
- [ ] 口径：从固定事件集计算出预期北极星与护栏指标，分母为 0 时不产生 NaN。
- [ ] 开关（接线后）：opt-in/opt-out、导出、清除；关闭遥测后无外部上报且不影响功能。
- [ ] 口径版本：指标带 `metricDefinitionVersion`，口径变化时版本可区分。

---

## R3 DELIV-03 — 可打开 / 预览 / 下载 / 版本化的 Artifact 消费体验  `[多格式预览已实现·版本与来源受契约缺口]`

**实现位置**
- 纯逻辑：`packages/app/modules/agent-company/runtime/shared/artifact-view.ts` — `resolveRenderMode`（按 mediaType/后缀/presentation 判定 markdown/code/json/csv/image/pdf/text/download）、`formatByteLength`、`isOversizedForInline`（512KB 阈值）、`prettyJson`（解析失败原样返回）、`parseCsvPreview`（限行 + truncated 标记）、`downloadFileName`、`dataUrl`。
- 页面：`packages/app/app/pages/library/artifacts/[projectID]/[artifactID].vue` — 按 renderMode 分别渲染（文本/代码/JSON 美化为 `<pre>`、CSV 为表格、图片为 `<img>`、PDF 为 `<iframe>`、其余降级为下载）；动作条提供下载（按真实后缀命名）与复制链接；超大文件不内联预览而引导下载。
- 样式：`packages/app/app/assets/css/main.css` — `.ac-artifact-actions/action/notice/table/table-wrap/pdf`。
- 数据源：复用已有真实代理 `/api/agent-company/projects/:projectID/artifacts/:artifactID`（`artifact.get.ts` → control-plane `/experience/.../artifacts/...`，404/422/401 均有真实错误态）。

**开发者自测结论**：`artifact-view.ts` 新增 10 个纯逻辑单测；`bun run test:unit` → **151 pass / 0 fail across 23 files**；`bun typecheck` → **EXIT=0**。

**本任务未虚构的后端缺口（如实标注）**
- 版本化：`ExperienceArtifactView` 契约无多版本历史字段，“查看版本 / 回退版本”无法实现（归 DELIV-06 + 后端）。
- 生成来源：契约无 provenance/生成链路字段，“查看生成来源”未提供。
- Library 检索：当前 Library 仅列交付（按项目），尚无按类型/时间/状态的多维检索（需后端下发独立 Artifact 索引）。

### 待专人测试项
- [ ] 格式渲染：Markdown / 代码 / 图片 / PDF / CSV / JSON / 文本 各自按预期方式展示；CSV 表格列对齐，JSON 美化缩进。
- [ ] 异常态：文件不存在(404)/无法安全读取(422)/需重新授权(401) 均显真实错误与重读按钮，不展示猜测副本。
- [ ] 超大文件：超 512KB 的文本不内联渲染，改为提示下载，页面不卡死。
- [ ] JSON 损坏：非法 JSON 原样展示而非报错或伪装美化。
- [ ] 下载/复制：下载文件名带正确后缀；复制链接为可直达该 Artifact 的深链。
- [ ] 一致性：从 Project 交付区与 Library 打开同一 Artifact，内容一致。
- [ ] 版本/来源缺口确认：确认“查看版本/生成来源”当前未提供，且页面未伪装已实现（归 DELIV-06 + 后端）。

---

## R3 DELIV-05 — Delivery Package 与用户验收状态  `[前端展示与核对清单就绪·逐项结论与验收动作受契约/后端约束]`

**实现位置**
- 纯逻辑：`packages/app/modules/agent-company/runtime/shared/delivery-package.ts` — `acceptanceStateLabels`、`deliveryStage`（pending→delivered / accepted / revision_requested→revision）、`hasConsumableOutput`、`criterionVerdictLabels`、`acceptanceChecklist`（用 Goal Brief 验收标准构建核对项，verdict 恒为 unverified）、`deliveryPackageView`。
- 页面：`packages/app/app/pages/work/[projectID].vue` — 交付区區额外展示 Delivered/Accepted/Revision 状态徐、“已交付但尚未验收”提示、以及验收标准核对清单（逐项标 “未逐项核对”）。
- 样式：`packages/app/app/assets/css/main.css` — `.ac-status-badge[data-stage]`、`.ac-delivery-hint`、`.ac-acceptance*`。

**开发者自测结论**：`delivery-package.ts` 新增 6 个纯逻辑单测；`bun run test:unit` → **151 pass / 0 fail**；`bun typecheck` → **EXIT=0**。

**契约/后端驱动的诚实边界（如实标注）**
- `DeliverySummary` 契约仅下发整体 `acceptanceState`，**无逐项 pass/fail/partial/na 状态**、**无 Reviewer 发现 / 已知限制 / 使用说明**；核对清单因此一律标 unverified，绝不伪造通过。
- “接受交付 / 请求修改”属变更类动作，R0 契约下（`ExperienceR0ImplementedMutationActions=[]`）恒为 disabled；本任务仅提供状态展示与核对清单，验收动作本身由 WORK-07/DELIV-04 动作条如实禁用渲染。

### 待专人测试项
- [ ] 状态区分：pending 显“待验收”且提示待决策，accepted 显“已验收”无待决策提示，revision_requested 显“已请求修改”。
- [ ] 核对清单：列出最初 Goal Brief 的全部验收标准与验证方式；每项显 “未逐项核对”，不伪装已通过。
- [ ] 完整性：无成果时不得展示为已交付（契约已强制至少一个真实成果）。
- [ ] 后端缺口确认：逐项 pass/fail/partial/na、Reviewer 发现、已知限制、使用说明当前均未由后端下发，页面未虚构。
- [ ] 一致性（验收动作接线后）：接受交付后 Project/Work/Inbox/Library 状态同步。

---

## R3 DELIV-06 — 基于验收标准的返工 / 版本差异 / 证据与限制  `[纯逻辑就绪·变更类与版本差异受契约/后端全面约束]`

**实现位置**：`packages/app/modules/agent-company/runtime/shared/revision-model.ts`（纯逻辑）。
- 统一可区分的返工来源：`revisionOrigin`（user_request_change / reviewer_reject）+ `revisionOriginLabels`。
- 状态机：`DeliveryLifecycleState`（delivered→revision→reviewing→delivered→accepted）、`nextStatesFor`、`canTransition`（accepted 为终态）。
- 返工请求与版本差异：`isActionableRevision`（必须关联具体目标 + 带说明）、`hasDisplayableDelta`（需版本递增且至少一类变化条目）。

**开发者自测结论**：`revision-model.ts` 新增 5 个纯逻辑单测；`bun run test:unit` → **151 pass / 0 fail**；`bun typecheck` → **EXIT=0**。

**未接线/受约束（如实标注，未虚构 UI）**
- 请求修改属变更类动作，R0 契约下恒为 disabled；本模块仅提供“合法流转判定 + 展示模型”，不代表返工已可执行。
- 版本差异需后端下发多版本 Artifact 列表（`ExperienceArtifactView` 无版本历史字段），故 diff / 回退 / 证据与限制展示未在 UI 中构建。
- 本任务交付**返工模型纯逻辑 + 单测**，为后端能力就绪后的 UI 接线预备可验证基座。

### 待专人测试项
- [ ] 状态机：Delivered → Revision → Reviewing → Delivered → Accepted 合法流转；非法跳转被拒绝；accepted 为终态。
- [ ] 返工范围：无关联目标或无说明的请求不成立（避免泛泛返工）。
- [ ] 版本差异（后端接线后）：无版本递增或无变化条目时不展空 diff；内容/证据/限制指向正确版本。
- [ ] 来源区分：Reviewer 退回与用户请求修改共用统一但可区分的返工模型。
- [ ] 契约门禁回归：确认请求修改在 R0 未解除时为 disabled；放开后需补 request_change/gate-resolve 代理与多版本下发。

---

## R3 QA-01 — 核心用户成果闭环 E2E  `[新增 R1–R3 流程 E2E 已编写·待专人执行；变更类闭环受契约约束]`

**实现位置**：`packages/app/e2e/r1-r3-flows.spec.ts`（3 条用例，标签 `@r1-r3-flows`）；运行脚本 `bun run test:r1-r3`（复用现有 `playwright.config.ts` + `e2e/fake-control-plane.ts` 可控测试 Runtime，`ready` 模式稳定数据）。
- `@scenario-deliv05`：`project-delivered` 交付包以结果为中心——“待验收”状态徽章（data-stage=delivered）、待决策提示、成果链接指向 Artifact 详情、由 Goal Brief 生成的验收标准核对清单（2 项，均“未逐项核对”，不伪造 pass）。
- `@scenario-deliv03`：打开 `artifact-report` 校验 Markdown 内联预览 + 正文真实内容 + 下载/复制链接动作 + mediaType 元信息。
- `@scenario-deliv03`（异常）：访问不存在的 `artifact-missing` 时如实报“成果暂时不可用”，不出现虚假成功正文。

**开发者自测结论**：`bun typecheck` → **EXIT=0**；`bun run test:unit` → **151 pass / 0 fail**。E2E 用例本身需 Playwright + Nuxt dev server + fake-control-plane 联动，**未在开发环境执行**，留待专人运行（用户已明确安排）。

**未接线/受约束（如实标注）**
- 受契约 `ExperienceR0ImplementedMutationActions = []` 约束，Accept / 请求修改 / Gate 批准拒绝等变更类闭环无法端到端走通；本套件只覆盖只读消费路径，变更类断言留待后端解除门禁后补齐。
- 断线恢复 / Provider 错误 / 服务重启恢复等路径已由既有 `@r0-shell @scenario-s05` 覆盖；返工路径依赖 DELIV-06 后端能力，暂缺。

### 待专人测试项
- [ ] 执行 `bun run test:r1-r3`，确认 3 条用例全绿，失败时保存截图 / 控制台 / 网络 / trace（config 已开启 retain-on-failure）。
- [ ] 将核心 E2E 接入 CI 合并阻断（当前 CI 仅 grep `@r0-shell`；需扩展到 `@r1-r3-flows`）。
- [ ] 主路径（接线后）：首次用户完成研究/代码任务并接受交付。
- [ ] 恢复路径：运行中重启 Control Plane 后继续并交付。
- [ ] 真实性路径：后端断开时不出现虚假成功数据（已由 r0-shell 覆盖，建议纳入回归）。

---

## R3 QA-03 — 视觉回归与交互状态审查  `[基线截图已在 E2E 内产出·差异比对与签署流程待专人建设]`

**现状**：`bun run qa:visual`（`script/visual-qa.ts`）已做页面宽度/横向溢出检查；`@r0-shell` 与新增 `@r1-r3-flows` 用例在关键页面调用 `screenshotFromTop` 产出全页截图（shell-desktop / first-run-empty / delivery-package / artifact-markdown 等），落在 `.artifacts/e2e/`。

### 待专人测试项（属测试基础设施 + 人工签署，非纯代码）
- [ ] 为 First-run、Inbox、Goal Brief、Running、Blocked、Gate、Delivery、Team 建立截图基线，覆盖空/正常/错误/长文本/窄屏。
- [ ] 引入 before/after 差异比对与设计/责任人批准闸门；关键视觉改动无截图证据不得合并。
- [ ] 1440 / 1024 / 768 三档进入自动基线。
- [ ] 故意改间距/隐藏按钮/改错误态，验证回归能检出；动态数据冻结后连续两次结果稳定。

---

## R3 QA-04 — 核心路径 WCAG 2.2 AA 可访问性  `[语义/键盘基线已随组件建立·自动 axe 扫描与 SR 抽检待专人]`

**现状**：FND-04/SHELL-04 已落地语义标题层级、`aria-current`、`aria-live` 区域、可聚焦动作（既有 E2E 已断言导航 `aria-current` 与 focus）。**尚未引入** `@axe-core/playwright` 依赖，无自动无障碍扫描。

### 待专人测试项（需新增测试依赖 + 人工屏幕阅读器）
- [ ] 引入 axe 自动扫描 First-run / Inbox / Work / Gate / Delivery / Settings，无 serious/critical 问题。
- [ ] 仅键盘完成 Goal 输入、打开 Artifact；批准/接受/返工待变更类动作接线后补。
- [ ] NVDA 或 VoiceOver 至少一种走完核心路径，识别状态/主要动作/异步结果。
- [ ] 200% 缩放、减少动画偏好、AA 对比度校验。

---

## R3 QA-05 — 性能、长会话与恢复可靠性预算  `[待专人建设负载 Fixture 与性能预算门禁]`

**现状**：无大规模负载 Fixture（1000 Work / 100 Agent / 10000 事件），无性能预算 CI 门禁。`ref/opencode/…/e2e/performance` 有可参考的 Playwright 性能探针骨架，但未接入本产品。

### 待专人测试项（属性能测试基础设施 + 度量）
- [ ] 首个有意义内容 p95 ≤ 2.5s；点击本地反馈 ≤ 100ms；状态投影到 UI p95 ≤ 500ms。
- [ ] 大列表/长 Thread/多 Artifact 负载下核心操作可用；长会话与切项目无无界内存增长。
- [ ] 服务重启后自动恢复当前工作，不丢事件、不重复副作用（恢复成功率 100%）。
- [ ] 性能预算进入 CI 或发布检查，超标有明确报告。

---

## R3 QA-07 — 可用性验收、旧数据迁移与发布 Go/No-Go  `[发布验收清单已备·人工用户测试与迁移演练待专人]`

**现状**：本留档文件即为“发布审计逐项核验”的证据索引（R1–R3 各任务实现位置 + 自测结论 + 后端缺口）。可用性测试、迁移演练、回滚演练均为人工/发布活动，非代码交付。

### 待专人测试项（人工主持 + 发布流程）
- [ ] 招募 ≥5 名符合首要 Persona 的用户，无引导完成首次配置 / Goal / 干预 / Delivery / 返工；完成率目标 100%，SUS 中位数 ≥ 75。
- [ ] 建立 Severity 0–3 分级与发布阻断规则；无 Sev0/1 未解决。
- [ ] 迁移演练：≥2 个历史数据库版本与旧路由书签（注意 AGENTS.md：本产品不承诺 legacy AgentCompany 兼容，迁移仅限显式要求范围）。
- [ ] 回滚演练：新版本失败后恢复旧版本并验证数据不丢。
- [ ] 发布审计：逐项核验本计划验收证据、截图、测试报告与指标后，方可标记重构完成。

